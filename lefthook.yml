pre-commit:
  parallel: true
  commands:
    biome-check:
      run: npx biome check --write .

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}

pre-push:
  commands:
    build:
      run: npm run build